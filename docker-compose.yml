version: "3.8"
services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    volumes:
      - ./backend/faces.db:/app/faces.db

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
